---
layout: null
permalink: /sitemap.xml
sitemap:
  include: false
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd" xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% assign pages = site.pages %}
  {% assign posts = site.posts %}
  {% assign data = pages | concat: posts %}  
  {% assign default_time = site.time | date_to_xmlschema %}
  
  {%- if site.sitemap.enable != false -%}
    {% for page in data %}

      {%- if page.url contains '/@reference/' or page.layout contains 'redirect' or page.layout.sitemap.include == false or page.sitemap.include == false -%}
        {%- continue -%}
      {%- endif -%}

      <url>
        <loc>{{ site.url }}{{ page.url }}</loc>

        {%- if page.sitemap.lastmod == null -%}
          <lastmod>{{ default_time }}</lastmod>
        {%- else -%}
          <lastmod>{{ page.sitemap.lastmod | date_to_xmlschema }}</lastmod>
        {%- endif -%}

        {%- if page.sitemap.changefreq == null -%}
          <changefreq>weekly</changefreq>
        {%- else -%}
          <changefreq>{{ page.sitemap.changefreq }}</changefreq>
        {%- endif -%}

        {%- if page.url == '/' -%}
          <priority>1.0</priority>
        {%- elsif page.sitemap.priority == null -%}
          <priority>0.5</priority>          
        {%- else -%}
          <priority>{{ page.sitemap.priority }}</priority>
        {%- endif -%}
      </url>
    {% endfor %}
  {%- endif -%}  
</urlset>
